apiVersion: v1
kind: Service
metadata:
  name: user-activity-service
spec:
  type: NodePort
  selector:
    app: uuser-activity-logger
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007